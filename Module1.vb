Module Module1

    Sub Main()
        Dim inputStr As String
        Console.Write("Enter a string: ")
        inputStr = Console.ReadLine()

        Dim result As Boolean

        If result Then
            Console.WriteLine("The input string contains all the alphabets.")
        Else
            Console.WriteLine("The input string does not contain all the alphabets.")
        End If

        Console.ReadLine()
    End Sub

End Module
